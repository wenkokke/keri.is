{
  "private": true,
  "name": "keri.is",
  "scripts": {
    "build": "npm run build:card && npm run build:experimental-jams",
    "build:card": "npm run build --workspace=card",
    "build:experimental-jams": "npm run build --workspace=experimental-jams"
  },
  "workspaces": [
    "card",
    "experimental-jams",
    "parcel-transformer-pandoc"
  ],
  "parcel-namer-rewrite": {
    "chain": "@parcel/namer-default",
    "hashing": "always",
    "rules": {
      "index.html$": "index.html",
      "(.*).html$": "$1/index.html"
    }
  }
}
